name: gas
description: A new Flutter project.

environment:
  sdk: '^3.4.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2


  
  # Add other dependencies as needed




dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - Assets/logo.png
    - Assets/googleIcon.png
    - Assets/facebook.png
